server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://172.17.0.2:5432/ead_course
    username: postgres
    password: 123@
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect/PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true
